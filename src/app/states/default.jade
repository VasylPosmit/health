div.states(layout= "column", layout-align= "space-between")
  section(flex)
    kil-toolbar
    md-content.content(flex)
      kil-section(statedata='layout.data')
      md-divider
      block input
        h2 Recommendations generator
        div(layout-sm='column', layout)
          div(flex-gt-sm= "25")
            h3 What is it
            p Health guide based on provided information elaborate recommendations that is appropriate just for you.
          div(flex-gt-sm= "40", flex-offset-gt-sm="5")
            h3 How it works
            p Health guide database contains hundreds of recommendations each of those correspond only to some narrow group of people. Engine use the information you provide to link your profile with bunch of groups and suitable recommendations
          div(flex-gt-sm= "25", flex-offset-gt-sm="5")
            h3 How to use
            p Enter information.
            p Press "CALCULATE RECOMMENDATIONS"
            p Check recommendations that is right just for you

        div(layout-gt-sm="row",  layout-xs="column", layout-align='space-between center', flex-gt-md="50")
          md-input-container.md-block(flex, flex-gt-sm='60')
            label Name
            input(ng-model='layout.user.name', ng-model-options="{ updateOn: 'blur' }")
          md-radio-group( flex, flex-gt-sm="35", layout, ng-model="layout.user.isMale")
            //- label Gender
            md-radio-button.md-primary(ng-value= "true") Man
            md-radio-button.md-primary(flex-offset= '15', ng-value= "false") Woman
        md-input-container.md-block(flex-sm= '50', flex-gt-sm='20')
            label age
            input(min='18', max='99', type='number', ng-model='layout.user.age', aria-label='age', aria-controls='weight-slider')
      kil-recommendations

  footer(layout)
    md-content.content(layout, layout-align-xs= "center center", layout-align= "start center", flex-gt-xs= "66", flex)
      p(flex, flex-gt-md = '33') Developed by Vasyl Posmit ©
      div(layout, layout-gt-sm= "end center", flex-gt-md = '30', flex)
        p(flex)
          a(href="https://www.facebook.com/posmit", layout, layout-align="center center")
            i.material-icons chat
            span(flex-offset="5") Contact author
        p(flex)
          a(href="https://github.com/VasylPosmit/health", layout, layout-align="center center")
            i.material-icons code
            span(flex-offset="5") Source code
